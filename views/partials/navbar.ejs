<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Foodie Ground</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-between" id="navbarNav">
        <div class="navbar-nav">
        <ul class="navbar-nav">
          <% if(currentUser){ %> 
 
          <% if(currentUser.accountType === 'owner') {%> 
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/users/<%= currentUser._id %>/mystores">My Stores</a>
          </li>
          <% }else{ %> 
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="/stores">Home</a>
            </li>
            <% } %> 

          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/users/<%= currentUser._id %>/favorites">Favorites</a>
          </li>

          <% if(currentUser && currentUser.accountType === 'owner') {%> 
          <li class="nav-item">
            <a class="nav-link" href="/stores/new">New Store</a>
          </li>
          <% } %> 
          <% } %>
        </ul>
      </div>
        <div class="navbar-nav">
          <% if(!currentUser) {%> 
          <a class='nav-link' href="/users/register">Register</a>
          <a class='nav-link' href="/users/login">Login</a>
          <% }else { %> 

          <a class='nav-link  py-1 mx-3 active' href="/users/<%= currentUser._id %>"> 
            <mdb-navbar-brand href="" target="_blank">
              <img alt="profile" width="45" height="40" style="border-radius: 50%; box-shadow: 0 0 10px 1px rgba(253, 253, 253, 0.428);" src="<%= currentUser.profileImage.thumbnail %> "/>
            </mdb-navbar-brand>
            <%= currentUser.username %> 
          </a>

          
          <a class='nav-link active' href="/users/logout">Logout</a>
          <% } %> 
        </div>
      </div>
    </div>
  </nav>
